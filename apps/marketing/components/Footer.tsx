// The marketing footer is the shared SiteFooter (packages/ui), reused verbatim
// by the telemetry dashboard so the two surfaces share one footer.
export { SiteFooter as Footer } from '@livediagram/ui';
