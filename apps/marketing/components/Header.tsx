// The marketing landing header is the shared SiteHeader (packages/ui), with no
// apps-menu dropdown so the landing page keeps a plain wordmark. The telemetry
// dashboard renders the same SiteHeader with the menu enabled.
export { SiteHeader as Header } from '@livediagram/ui';
