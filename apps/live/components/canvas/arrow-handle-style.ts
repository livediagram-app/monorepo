// Brand-600, the editor's selection / handle accent. Shared by the arrow
// endpoint / curve / add-point handles (ArrowView) and the arrow label's
// edit box (ArrowLabel) so the two can't drift to different blues.
export const BRAND_600 = 'rgb(2 132 199)';
