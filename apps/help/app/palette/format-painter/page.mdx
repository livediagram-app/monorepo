import { ArticleLayout } from '@/components/ArticleLayout';
import { getSubArticles } from '@/lib/articles';
import { helpMetadata } from '@/lib/seo';
import { FormatPainterMode } from '@/components/illustrations/palette-modes';

export const metadata = helpMetadata({
  title: 'Format Painter',
  description:
    "Use the Format Painter mode to copy one element's style and paint it onto others, keeping a diagram visually consistent in a couple of clicks.",
  path: '/help/palette/format-painter/',
});

<ArticleLayout
  title="Format Painter"
  description="Copy one element's style and paint it onto other elements, keeping a diagram visually consistent."
  categoryTitle="Palette"
  categorySlug="palette"
  subArticles={getSubArticles("format-painter")}
>

The Format Painter mode copies the look of one element (its colours, line, and text styling) and applies it to others, so a set of shapes can share a consistent style without setting each one by hand. Choose it from the mode picker at the top of the floating palette.

## How to use it

<Step n={1}>Pick the Format Painter mode from the palette.</Step>
<Step n={2}>Click a source element to copy its look.</Step>
<Step n={3}>Click each element you want to apply that style to.</Step>

Every element you click takes on the copied colours, line, and text styling. Because it copies the styling rather than the content, each target keeps its own text and position, only its look changes.

<Figure caption="The Format Painter is lit in the mode picker, copying the source shape's style onto another.">
  <FormatPainterMode />
</Figure>

## Painting across many elements

The format painter is most useful when you have several elements to bring into line. Combine it with a [multi-selection](/help/canvas/selecting-and-grouping/multi-select/): gather the elements that need the same style, then paint the source look across the whole selection at once. A row of mismatched boxes becomes a tidy, uniform set in a single action.

## Format painter and themes

The format painter copies the exact look of one element onto another, which is perfect for matching a handful of shapes. When you want to restyle the entire diagram at once, reach for a [theme](/help/canvas/themes/) instead, which recolours every element together. And for quick emphasis on a single shape, the [style presets](/help/palette/shapes/style-presets/) in the right-click menu offer one-click colour and border variations derived from the active theme.

<Tip>
  Style one element carefully, then let the format painter do the repetitive work. It is far quicker
  than matching colours and borders by eye across a busy canvas.
</Tip>

</ArticleLayout>
