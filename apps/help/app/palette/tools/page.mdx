import { ArticleLayout } from '@/components/ArticleLayout';
import { getSubArticles } from '@/lib/articles';
import { helpMetadata } from '@/lib/seo';

export const metadata = helpMetadata({
  title: 'Tools',
  description:
    'The palette Tools tab gathers everything that is not a plain shape: text, freehand drawing, arrows, sticky notes, tables, images, frames, and a Data sub-section of charts and meters.',
  path: '/help/palette/tools/',
});

<ArticleLayout
  title="Tools"
  description="The palette tab for everything that is not a plain shape, from text and freehand sketches to images and charts."
  categoryTitle="Palette"
  categorySlug="palette"
  subArticles={getSubArticles("tools")}
>

The palette's **Tools** tab gathers the elements that are not plain shapes. It is split into two sub-sections: **Tools** for general-purpose elements, and **Data** for charts and meters.

## The Tools sub-section

This sub-section holds the everyday building blocks. **Text** drops a free-standing label. **Pencil** lets you sketch freehand, with an optional shape-recognition mode. **Arrow** drops a plain connector you can position by hand. **Avatar** places a circular image and **User** places an actor figure, both handy for people in a flow.

A few more are self-explanatory: **Sticky Note** is a coloured note card, **Table** is an editable grid of cells, **Frame** is a labelled container you group work inside, **Annotation** is a small note marker for callouts, **Link Card** is a clickable preview of a URL, and **Timeline Rail** is a horizontal track for sequencing events. Each behaves like any other element once placed: select, restyle, group, and move it freely.

## Guides in this tab

Several tools have their own walkthroughs, listed below and linked here:

- [Drawing and Sketch](/help/palette/tools/drawing/) covers the Pencil tool and shape recognition.
- [Images](/help/palette/tools/images/) covers uploading and placing pictures, avatars, and user figures.
- [Data and Chart Elements](/help/palette/tools/data-elements/) covers the Data sub-section: Pie Chart, Bar Chart, Line Chart, Progress Bar, Progress Ring (a donut meter), and Rating (stars).

The **Arrow** tool lives here too, but arrows are deep enough to deserve their own guide: see [Arrows](/help/palette/arrows/).

<Tip>
  Reach for a Frame when a diagram grows past one screen: it keeps a section labelled and easy to
  move as a unit.
</Tip>

<FeatureGrid>
  <Feature slug="arrows" title="Arrows">
    Connect elements with arrows of every style, with draggable curve and elbow handles.
  </Feature>
  <Feature slug="shapes" title="Shapes">
    The plain rectangles, circles, and more that the Tools tab complements.
  </Feature>
</FeatureGrid>

</ArticleLayout>
