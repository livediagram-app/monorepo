import { ArticleLayout } from '@/components/ArticleLayout';
import { getSubArticles } from '@/lib/articles';
import { helpMetadata } from '@/lib/seo';
import { FormatPainter } from '@/components/illustrations/canvas';

export const metadata = helpMetadata({
  title: 'Using the Format Painter',
  description:
    "Copy one element's look and paint it onto others, so a whole diagram shares a consistent style without re-setting each field.",
  path: '/help/canvas/selecting-and-grouping/using-the-format-painter/',
});

<ArticleLayout
  title="Using the Format Painter"
  description="Pick up the look of a styled element and brush it onto others, keeping a diagram visually consistent in a couple of clicks."
  categoryTitle="Canvas"
  categorySlug="canvas"
  parentArticle={{ title: "Selecting and Grouping", slug: "selecting-and-grouping" }}
  subArticles={getSubArticles("selecting-and-grouping").filter((a) => a.slug !== "using-the-format-painter")}
>

Once you have styled one shape just the way you want, with the right fill, border, and text, you rarely want to set all those fields again by hand on the next shape. The format painter copies a styled element's look and applies it to others, so a whole diagram can share one consistent style without repetitive fiddling.

## How it works

The format painter works in two steps: pick up a look, then put it down.

<Step n={1}>
  Select the element whose style you want to reuse, the one you have already got looking right.
</Step>

<Step n={2}>
  Activate the format painter to pick up its look, then click the element you want to restyle. The
  target takes on the source element's appearance.
</Step>

Because it copies the styling rather than the content, the target keeps its own text and position. Only its look changes.

<Figure caption="The format painter lifts a styled element's look and applies it to the target, keeping the target's own text.">
  <FormatPainter />
</Figure>

## Painting across many elements

The format painter is most useful when you have several elements to bring into line. Combine it with a [multi-selection](/help/canvas/selecting-and-grouping/multi-select/): gather the elements that need the same style, then paint the source look across the whole selection at once. A row of mismatched boxes becomes a tidy, uniform set in a single action.

## Format painter and themes

The format painter copies the exact look of one element onto another, which is perfect for matching a handful of shapes. When you want to restyle the entire diagram at once, reach for a [theme](/help/canvas/themes/) instead, which recolours every element together. And for quick emphasis on a single shape, the [style presets](/help/palette/shapes/style-presets/) in the right-click menu offer one-click colour and border variations derived from the active theme.

<Tip>
  Style one element carefully, then let the format painter do the repetitive work. It is far quicker
  than matching colours and borders by eye across a busy canvas.
</Tip>

</ArticleLayout>
