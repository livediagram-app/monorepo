import { ArticleLayout } from "@/components/ArticleLayout";
import { getSubArticles } from "@/lib/articles";
import { helpMetadata } from "@/lib/seo";

export const metadata = helpMetadata({
  title: "Templates",
  description:
    "Start from a themed template instead of a blank canvas, with the structure and styling already in place.",
  path: "/help/features/templates/",
});

<ArticleLayout
  title="Templates"
  description="Begin a diagram from a ready-made, themed starting point instead of an empty canvas, and adapt it to your needs."
  categoryTitle="Features"
  categorySlug="features"
  subArticles={getSubArticles("templates")}
>

A blank canvas is freeing, but sometimes you would rather start with structure already in place. Templates give you a ready-made diagram as a starting point, complete with a theme applied, so you can begin from something shaped roughly like what you need and adapt it, instead of building every diagram from nothing.

## Starting from a template

When you [create a new diagram](/new), you can pick a template rather than an empty canvas. Each template arrives with its elements laid out and a [theme](/help/features/themes/) already applied, so the look is coherent from the first moment. From there it is an ordinary diagram: move, edit, add, and delete elements exactly as you would on any canvas.

## Choosing a theme as you start

The new-diagram screen pairs templates with the same theme picker you see in the Tab Appearance dialog, organised into categories. You can swap the theme as you choose a template, including any of your [custom themes](/help/features/themes/custom-themes/), so the starting point matches your style before you have placed a single new element.

## Making it your own

A template is just a head start, not a constraint. Everything in it is fully editable: rename and recolour the elements, restyle the whole thing with a different theme, or strip out the parts you do not need. Because the diagram works the same as any other, you can also add [tabs](/help/features/tabs/), invite others, and share it once you have shaped it into what you want.

<Tip>Templates pair naturally with themes: pick a layout that fits your content, then try a few themes to find the look you want before you start editing.</Tip>

</ArticleLayout>
