import { ArticleLayout } from '@/components/ArticleLayout';
import { helpMetadata } from '@/lib/seo';
import { ZenMode, LaserPointer } from '@/components/illustrations/tips';

export const metadata = helpMetadata({
  title: 'Presenting from the Canvas',
  description:
    'Walk an audience through a diagram with zen mode and the laser pointer in livediagram.',
  path: '/help/tips-and-tricks/presenting-well/',
});

<ArticleLayout title="Presenting from the Canvas" description="Turn a finished diagram into a clear story with a distraction-free view and a laser pointer." categoryTitle="Tips and Tricks" categorySlug="tips-and-tricks">

A finished diagram shows everything at once, which is great for reference but harder to narrate. livediagram gives you a couple of ways to focus an audience as you walk through your work, whether you are on a call, a projector, or sharing your screen.

## Zen mode

When you want the full canvas with nothing else competing for attention, [Zen mode](/help/tools/zen-mode/) hides every part of the interface, the header, tab bar, palette, and panels, leaving only the canvas and zoom controls. It is purely a view setting: it changes nothing about the diagram and is not shared with other people, so each viewer focuses independently. Toggle it with `Z` (or the button on the zoom controls), and exit with `Escape`.

<Figure caption="Zen mode clears every panel, leaving just the diagram and the zoom controls.">
  <ZenMode />
</Figure>

## The laser pointer

For pointing things out live, switch to the **Laser** tool (`K`). Moving the pointer leaves a glowing trail in your participant colour that fades after about a second, and on a shared session other people see your laser in real time, so you can sweep across the diagram and direct the room's attention.

<Figure caption="The laser leaves a glowing trail in your colour that fades behind the pointer as you sweep.">
  <LaserPointer />
</Figure>

<Tip>
  Reveal a busy diagram gradually by building it up tab by tab, then switch to Zen mode and use the
  laser to walk the room through each step.
</Tip>

</ArticleLayout>
