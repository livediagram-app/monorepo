// The help-centre footer is the shared SiteFooter (packages/ui), the same one
// the marketing landing page and telemetry dashboard render, so every surface
// with a footer is of-a-piece.
export { SiteFooter as Footer } from '@livediagram/ui';
